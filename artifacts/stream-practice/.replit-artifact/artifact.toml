kind = "web"
previewPath = "/"
title = "StreamPractice AI"
version = "1.0.0"
id = "artifacts/stream-practice"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24685

[services.development]
run = "pnpm --filter @workspace/stream-practice run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/stream-practice", "run", "build" ]
publicDir = "artifacts/stream-practice/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24685"
BASE_PATH = "/"
